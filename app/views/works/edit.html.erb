<%= form_for @work, url: user_work_path(@user, @work), multipart: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <br>
  <%= f.label :attachment %>
  <%= f.file_field :attachment %>

  <br>

  <%= f.submit %>
<% end %>
